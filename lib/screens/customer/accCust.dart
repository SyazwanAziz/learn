import 'package:flutter/material.dart';

class AccountCust extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This account for customer page'),
    );
  }
}
