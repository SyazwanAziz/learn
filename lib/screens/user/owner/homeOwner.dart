import 'package:flutter/material.dart';
import 'package:learn/screens/user/owner/editService.dart';

class HomeOwner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
      child: Text('List of service'),
    ));
  }
}
